---
title: 持久化
lang: zh-CN
date: 2021-11-30
publish: true
author: azrng
isOriginal: true
category:
  - dataBase
tag:
  - 无
filename: chijiuhua
slug: ya96fr
docsId: '32327229'
---
持久化就是将内存中的数据放到硬盘上，redis持久化方式有两种，RDB方式(默认)和AOF两种

## 1. RDB保存(默认持久化方式)
RDB**快照**的形式存储，指定时间内操作多少步骤然后就做快照保存。每次保存的文件都比之前大，所以是增量方式。
默认配置：60秒内操作数达到1w次，那么就保存；5分钟内操作超过十次，那么就保存；15分钟内操作超过1条，那么就保存。
Save：通过命令save可以强制持久化保存为rdb文件，但是因为redis是单线程的，期间会阻塞redis服务进程，redis服务不再处理任何指令，除非等保存结束。
Bgsave：父进程启动一个子进程，然后通过子进程将内存中的数据持久化到文件中，期间不会影响其他的指令操作。
> 优点：通过快照去还原的时候快。    
> 缺点：极限情况下会因为还没有持久化而丢失数据。

触发机制--主要三种方式

- save 同步命令(会阻塞redis)
- bgsave 异步命令（fork）
- 自动

## 2. AOF保存
通过执行命令然后实现数据追加写入硬盘持久化。每执行一条命令，就在AOF中增加一条记录。当需要恢复时，直接执行AOF写入redis

### 2.1 三种追加方式
| 备注 | 类型 | 安全性 | 性能 |
| --- | --- | --- | --- |
| 等业务不繁忙的时候，这种操作最不可靠 | no | 低 | 高 |
| 1秒钟的周期 | everysec | 比较高 | 比较高 |
| 只要有读写 | always | 高 | 低 |

> 优点：持久化更安全
> 缺点：恢复时候比较慢，占用空间更大


## 3. 混合模式持久化
集成了RDB和AOF的优点，通过AOF方式持久化，然后等文件达到一定量大小的时候，然后将aof生成的文件改成rdb文件。

## 4. 区别
| 命令 | RDB | AOF |
| --- | --- | --- |
| 启动优先级 | 低 | 高 |
| 体积 | 小 | 大 |
| 恢复速度 | 快 | 慢 |
| 数据安全性 | 丢数据 | 根据策略决定 |
| 轻重 | 重 | 轻 |
| 备份方式 | 增量备份 | 全量备份 |

## 文档

https://mp.weixin.qq.com/s/w1FGA-7bl2EYtsVj36g__w | 深入解析Redis 持久化机制，面试要考额
